# git hub

# git add "filename"
# git commit - m "comment"
# git push 

# git log --oneline (This will give you the commit logs so far in one line)
# git diff (This is to see what was changed before comitting)

# Branches ---> Right now all code lives within the "main", but branches you create within that main a branch
# is like a paralle copy of your code where you can experiment, build a feature, or fix a bug without touching
# your main code and when you are ready with the changes you can merge it back to your main

# git branch "name of branch" (This will create a branch inside main)
# git checkout "name of branch" (This will take you to the branch)
# now repeat, the steps of adding and comitting a file and when you go back to main you will not see the file 
# you comitted to that branch

# ls --> is to see the list of files in that directory!

# git merge "name of branch" (this will merge the branch and its file to the main)

# git branch -d "name of the branch" (this will allow you to delete the branch as it has served its purpose)

